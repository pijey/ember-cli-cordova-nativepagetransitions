import { nptLinkTo } from '../../../helpers/npt-link-to';
import { module, test } from 'qunit';

module('Unit | Helper | npt link to');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = nptLinkTo(42);
  assert.ok(result);
});
